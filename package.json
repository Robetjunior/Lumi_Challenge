{
    "name": "lumi-challenge",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
      "api",
      "frontend"
    ],
    "scripts": {
      "install-all": "npm install --workspaces",
      "start-all": "concurrently \"npm run dev --workspace=api\" \"npm start --workspace=frontend\""
    },
    "devDependencies": {
      "concurrently": "^8.0.1"
    }
  }
  